<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Good Grouping</title>
  <link rel="manifest" href="/manifest.webmanifest">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <meta name="theme-color" content="#ff2d95">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-screen flex flex-col scrollbar-pub">
  <!-- Pub-style Navigation Bar -->
  <nav class="bg-pub-wood-dark border-b-4 border-pub-wood-medium sticky top-0 z-50 shadow-warm">
    <div class="max-w-4xl mx-auto px-4 py-3 flex justify-between items-center">
      <!-- Logo with Neon Effect -->
      <a href="/" class="flex items-center gap-3 group">
        <!-- Mini Dartboard Icon -->
        <div class="w-8 h-8 rounded-full border-2 border-pub-wood-light flex items-center justify-center bg-pub-green-900 group-hover:shadow-neon-pink-sm transition-all duration-300">
          <span class="text-neon-pink text-xs">🎯</span>
        </div>
        <span class="text-xl font-pub-heading font-bold text-neon-pink animate-neon-flicker hover:text-neon-blue transition-colors duration-300" style="text-shadow: 0 0 10px #ff2d95, 0 0 20px #ff2d95;">
          Good Grouping
        </span>
      </a>

      <% if (typeof user !== 'undefined' && user) { %>
        <div class="flex items-center gap-4">
          <!-- Live Games Link -->
          <a href="/live-games" class="text-aged-cream hover:text-neon-green transition-colors duration-200 text-sm font-pub-heading uppercase tracking-wide flex items-center gap-2">
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-neon-green opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-neon-green"></span>
            </span>
            <span class="hidden sm:inline">Live</span>
          </a>

          <!-- New Game Link -->
          <a href="/games/new" class="text-aged-cream hover:text-neon-blue transition-colors duration-200 text-sm font-pub-heading uppercase tracking-wide flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
            </svg>
            <span class="hidden sm:inline">New Game</span>
          </a>

          <% if (user.is_admin) { %>
            <!-- Admin icon for mobile -->
            <a href="/admin/users" class="sm:hidden text-aged-cream hover:text-neon-yellow transition-colors duration-200" title="Admin">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </a>
            <!-- Admin Links - visible on larger screens -->
            <a href="/admin/users" class="hidden sm:inline text-aged-cream hover:text-neon-yellow transition-colors duration-200 text-sm font-pub-heading uppercase tracking-wide">
              Users
            </a>
            <a href="/admin/invites" class="hidden sm:inline text-aged-cream hover:text-neon-yellow transition-colors duration-200 text-sm font-pub-heading uppercase tracking-wide">
              Invites
            </a>
          <% } %>

          <!-- Notification Bell -->
          <div class="relative" id="notification-dropdown">
            <button id="notification-bell" class="relative text-aged-cream hover:text-neon-blue transition-colors duration-200">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
              <% if (typeof unreadNotificationCount !== 'undefined' && unreadNotificationCount > 0) { %>
                <span id="notification-badge" class="notification-badge">
                  <%= unreadNotificationCount > 9 ? '9+' : unreadNotificationCount %>
                </span>
              <% } else { %>
                <span id="notification-badge" class="hidden notification-badge"></span>
              <% } %>
            </button>

            <!-- Notification Dropdown Panel -->
            <div id="notification-panel" class="hidden fixed sm:absolute right-2 sm:right-0 left-2 sm:left-auto mt-2 sm:w-80 card rounded-xl shadow-xl z-50" style="top: 3.5rem;">
              <div class="chalk-header rounded-t-lg flex justify-between items-center">
                <span class="font-pub-heading text-pub-chalk-text">Notifications</span>
                <div class="flex items-center gap-3">
                  <button id="clear-all-notifications" class="text-sm text-pub-chalk-accent hover:text-neon-orange transition-colors hidden">Clear</button>
                  <a href="/notifications" class="text-sm text-neon-blue hover:text-neon-pink transition-colors">View all</a>
                </div>
              </div>
              <div id="notification-list" class="max-h-96 overflow-y-auto scrollbar-pub p-2">
                <div class="p-4 text-center text-pub-chalk-accent">Loading...</div>
              </div>
            </div>
          </div>

          <!-- User Profile Link -->
          <a href="/profile" class="flex items-center gap-2 group">
            <% if (user.avatar_url) { %>
              <!-- Simple circle on mobile, polaroid on larger screens -->
              <img src="<%= user.avatar_url %>" alt="" class="rounded-full object-cover ring-2 ring-neon-green sm:hidden" style="width:32px;height:32px;min-width:32px">
              <div class="avatar-polaroid hidden sm:block">
                <img src="<%= user.avatar_url %>" alt="" class="w-8 h-8 rounded-polaroid object-cover">
              </div>
            <% } else { %>
              <div class="w-8 h-8 rounded-full bg-pub-green-700 flex items-center justify-center border-2 border-neon-green group-hover:shadow-neon-green transition-all duration-200">
                <span class="text-sm font-bold text-neon-green"><%= user.name.charAt(0).toUpperCase() %></span>
              </div>
            <% } %>
            <span class="text-pub-chalk-accent text-sm font-pub-heading group-hover:text-neon-green transition-colors duration-200 hidden sm:block">
              <%= user.name %>
            </span>
          </a>

          <!-- Logout Button -->
          <form action="/logout" method="POST" class="inline">
            <button type="submit" class="text-pub-chalk-accent hover:text-neon-orange transition-colors duration-200 text-sm font-pub-heading uppercase">
              Exit
            </button>
          </form>
        </div>
      <% } %>
    </div>
  </nav>

  <!-- Main Content Area -->
  <main class="flex-1 max-w-4xl mx-auto w-full px-4 py-8 pb-20 sm:pb-8">
    <%- body %>
  </main>

  <!-- Pub-style Footer -->
  <footer class="bg-pub-wood-dark border-t-4 border-pub-wood-medium py-6">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <div class="flex items-center justify-center gap-3 mb-2">
        <div class="h-px w-12 bg-gradient-to-r from-transparent to-pub-wood-light"></div>
        <span class="text-neon-pink font-pub-heading text-sm animate-neon-flicker" style="text-shadow: 0 0 5px #ff2d95;">
          🎯 Good Grouping 🎯
        </span>
        <div class="h-px w-12 bg-gradient-to-l from-transparent to-pub-wood-light"></div>
      </div>
      <p class="text-pub-chalk-accent text-xs font-pub-body">
        Est. 2024 • Your Local Darts Scoreboard
      </p>
      <button id="theme-toggle" onclick="toggleTheme()" class="mt-2 text-pub-chalk-accent text-xs hover:text-neon-blue transition-colors">
        ☀️ Light Mode
      </button>
      <script>
        function toggleTheme() {
          const html = document.documentElement;
          const btn = document.getElementById('theme-toggle');
          if (html.classList.contains('light')) {
            html.classList.remove('light');
            localStorage.removeItem('theme');
            btn.textContent = '☀️ Light Mode';
          } else {
            html.classList.add('light');
            localStorage.setItem('theme', 'light');
            btn.textContent = '🌙 Dark Mode';
          }
        }
        // Restore theme preference
        if (localStorage.getItem('theme') === 'light') {
          document.documentElement.classList.add('light');
          document.addEventListener('DOMContentLoaded', () => {
            const btn = document.getElementById('theme-toggle');
            if (btn) btn.textContent = '🌙 Dark Mode';
          });
        }
      </script>
    </div>
  </footer>

  <% if (typeof user !== 'undefined' && user) { %>
    <!-- Mobile Bottom Navigation -->
    <nav class="sm:hidden fixed bottom-0 left-0 right-0 bg-pub-wood-dark border-t-2 border-pub-wood-medium z-50 pb-safe">
      <div class="flex justify-around items-center py-2">
        <a href="/" class="flex flex-col items-center gap-0.5 text-pub-chalk-accent hover:text-neon-pink transition-colors px-3 py-1">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
          </svg>
          <span class="text-[10px]">Home</span>
        </a>
        <a href="/live-games" class="flex flex-col items-center gap-0.5 text-pub-chalk-accent hover:text-neon-green transition-colors px-3 py-1 relative">
          <span class="absolute top-0 right-1 flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-neon-green opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-neon-green"></span>
          </span>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
          <span class="text-[10px]">Live</span>
        </a>
        <a href="/games/new" class="flex flex-col items-center gap-0.5 text-pub-chalk-accent hover:text-neon-blue transition-colors px-3 py-1">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
          </svg>
          <span class="text-[10px]">New</span>
        </a>
        <a href="/crowns" class="flex flex-col items-center gap-0.5 text-pub-chalk-accent hover:text-neon-yellow transition-colors px-3 py-1">
          <span class="text-lg">👑</span>
          <span class="text-[10px]">Crowns</span>
        </a>
        <a href="/profile" class="flex flex-col items-center gap-0.5 text-pub-chalk-accent hover:text-neon-pink transition-colors px-3 py-1">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
          </svg>
          <span class="text-[10px]">Profile</span>
        </a>
      </div>
    </nav>
    <!-- Spacer for bottom nav -->
    <div class="sm:hidden h-16"></div>
  <% } %>

  <script src="/js/webauthn.js"></script>
  <script src="/js/notifications.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Dart Scores</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-screen flex flex-col bg-slate-900 text-slate-50">
  <nav class="bg-slate-800/80 backdrop-blur-sm border-b border-slate-700 sticky top-0 z-50">
    <div class="max-w-4xl mx-auto px-4 py-3 flex justify-between items-center">
      <a href="/" class="text-xl font-bold bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent hover:from-blue-300 hover:to-blue-500 transition-all duration-300">
        Dart Scores
      </a>
      <% if (typeof user !== 'undefined' && user) { %>
        <div class="flex items-center gap-4">
          <a href="/games/new" class="text-slate-300 hover:text-blue-400 transition-colors duration-200 text-sm font-medium">
            New Game
          </a>
          <% if (user.is_admin) { %>
            <a href="/admin/invites" class="text-slate-300 hover:text-blue-400 transition-colors duration-200 text-sm font-medium">
              Invites
            </a>
          <% } %>
          <!-- Notification Bell -->
          <div class="relative" id="notification-dropdown">
            <button id="notification-bell" class="relative text-slate-300 hover:text-blue-400 transition-colors duration-200">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
              <% if (typeof unreadNotificationCount !== 'undefined' && unreadNotificationCount > 0) { %>
                <span id="notification-badge" class="absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center font-medium">
                  <%= unreadNotificationCount > 9 ? '9+' : unreadNotificationCount %>
                </span>
              <% } else { %>
                <span id="notification-badge" class="hidden absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center font-medium"></span>
              <% } %>
            </button>
            <!-- Notification Dropdown -->
            <div id="notification-panel" class="hidden absolute right-0 mt-2 w-80 bg-slate-800 rounded-xl border border-slate-700 shadow-xl z-50">
              <div class="p-4 border-b border-slate-700 flex justify-between items-center">
                <span class="font-semibold text-slate-200">Notifications</span>
                <a href="/notifications" class="text-sm text-blue-400 hover:text-blue-300">View all</a>
              </div>
              <div id="notification-list" class="max-h-96 overflow-y-auto">
                <div class="p-4 text-center text-slate-500">Loading...</div>
              </div>
            </div>
          </div>
          <a href="/profile" class="flex items-center gap-2 group">
            <% if (user.avatar_url) { %>
              <img src="<%= user.avatar_url %>" alt="<%= user.name %>" class="w-8 h-8 rounded-full object-cover ring-2 ring-slate-600 group-hover:ring-blue-500 transition-all duration-200">
            <% } else { %>
              <span class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-blue-700 flex items-center justify-center text-sm font-semibold text-white ring-2 ring-slate-600 group-hover:ring-blue-400 transition-all duration-200">
                <%= user.name.charAt(0).toUpperCase() %>
              </span>
            <% } %>
            <span class="text-slate-400 text-sm group-hover:text-slate-200 transition-colors duration-200 hidden sm:inline">
              <%= user.name %>
            </span>
          </a>
          <form action="/logout" method="POST" class="inline">
            <button type="submit" class="text-slate-500 hover:text-rose-400 transition-colors duration-200 text-sm">
              Logout
            </button>
          </form>
        </div>
      <% } %>
    </div>
  </nav>

  <main class="flex-1 max-w-4xl mx-auto w-full px-4 py-8">
    <%- body %>
  </main>

  <footer class="bg-slate-800/50 border-t border-slate-700/50 py-4">
    <div class="max-w-4xl mx-auto px-4 text-center text-slate-500 text-sm">
      Dart Score Sharing
    </div>
  </footer>

  <script src="/js/webauthn.js"></script>
  <script src="/js/notifications.js"></script>
</body>
</html>

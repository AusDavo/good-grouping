services:
  app:
    build: .
    container_name: good-grouping-app-1
    # ports:
    #   - "3000:3000"
    networks:
      - caddy_rev-proxy
    volumes:
      - dart-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-change-this-to-a-secure-random-string}
      - RP_NAME=${RP_NAME:-Good Grouping}
      - RP_ID=${RP_ID:-localhost}
      - ORIGIN=${ORIGIN:-http://localhost:3000}
      - BASE_URL=${BASE_URL:-http://localhost:3000}
      - TRUST_PROXY=${TRUST_PROXY:-true}
    restart: unless-stopped

volumes:
  dart-data:
networks:
  caddy_rev-proxy:
    external: true
